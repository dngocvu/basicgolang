package rectangle

//
func AreaRect(length, width float64)(areaRectangle float64){
	areaRectangle = length * width
	return
}
//
func ParimeterRect(length, width float64)(parimeterRectangle float64){
	parimeterRectangle = (length + width)*2
	return
}